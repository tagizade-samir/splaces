enum AuthRoutes {
  login = 'login',
  signup = 'signup',
}

enum Routes {
  home = 'home',
  mapView = 'mapView',
  profile = 'profile',
  explore = 'explore',
}

enum SecureStorageKeys {
  isAuth = 'isAuth',
}

export {Routes, SecureStorageKeys, AuthRoutes}
